#include <bits/stdc++.h>
#define MAXS 100007

typedef long long ll;
using namespace std;

int main(){
  int n, m, lista[MAXS];
  while(scanf("%d %d", &n, &m), n != 0 && m != 0){
    for(int i = 0; i < n; i++){
      scanf("%d", &lista[i]);
    }
    sort(lista, lista + n);

    
    // 6 5 3 3 2
    // 0 1 3 3 4 
  }
}