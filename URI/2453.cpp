#include <bits/stdc++.h>
using namespace std;

int main(){
    string x;
    getline(cin, x);
    int cont =0;
    for(int i=x.size() -1; i > -1; i -= 2){
        if 
        cout << x[i + cont];
    }
}