#include <bits/stdc++.h>
using namespace std;

int main(){
    double a, b, c, med;
    scanf("%lf %lf %lf", &a, &b, &c);
    med = a * 0.2 + b * 0.3 + c * 0.5;
    printf ("MEDIA = %.1lf\n", med);
    return 0;
}   