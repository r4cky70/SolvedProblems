#include <bits/stdc++.h>
#define MAXS 507

using namespace std;
typedef long long ll;
typedef vector < int > vi;
typedef pair < int, int > ii;

int main(){
  int n;
  scanf("%d", &n);
  for(int i = 0; i < n; i++){
    int m;
    scanf("%d", &m);
    printf("%d\n", m / 2);
  }
}